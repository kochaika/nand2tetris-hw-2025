CHIP Mux {
    IN a, b, sel;
    OUT out;

    PARTS:
    Not(in= sel, out= notSel);
    And(a= a, b= sel, out= AandnotSel);
    And(a= b, b= sel, out= BorSel);
    Or(a= AandnotSel, b= BorSel, out= out);
}
